# Liste des tâches - Déploiement Agent Antigravity sur Vercel

## 1. Configurations de Vercel
- [ ] Modifier `vercel.json` pour configurer le builder Python `@vercel/python` sur l'endpoint `/api/analyse-patrimoniale.py`.
- [ ] Créer `frontend/api/requirements.txt` avec les dépendances Python pour Vercel.

## 2. Code de la fonction serverless Python
- [ ] Créer `frontend/api/analyse-patrimoniale.py` comme fonction serverless Vercel.

## 3. Synchronisation & Git
- [ ] Synchroniser toutes les modifications avec les dossiers Desktop.
- [ ] Faire un commit git et pousser sur la branche distante (`git push`).
- [ ] Vérifier que le déploiement sur Vercel s'effectue correctement.
